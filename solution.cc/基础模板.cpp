// == Code Location ==
#include <cstdio>
#include <vector>

template <typename T> using vec = std::vector<T>;

void solve(void) {}

int main() {
  freopen("data.in", "r", stdin);
  solve();
  fclose(stdin);
  return 0;
}